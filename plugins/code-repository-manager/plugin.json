{
  "schemaVersion": "expertmesh.plugin/v1",
  "id": "code-repository-manager",
  "name": "Code Repository Manager",
  "description": "Injects configured Git repositories as Agent documents and exposes tools for preparing Git and cloning repositories into the workspace.",
  "version": "0.0.0",
  "tags": ["git", "repository", "workspace"],
  "runtime": {
    "type": "expert-agent-plugin",
    "entry": "./dist/index.js"
  },
  "requires_env": [
    {
      "name": "EXPERTMESH_PLUGIN_CODE_REPOSITORY_MANAGER_GIT_TOKEN",
      "description": "Optional HTTPS Git token used by the code repository manager when cloning private repositories.",
      "required": false,
      "secret": true
    },
    {
      "name": "EXPERTMESH_PLUGIN_CODE_REPOSITORY_MANAGER_GIT_USERNAME",
      "description": "Optional HTTPS Git username paired with the token. Defaults to x-access-token.",
      "required": false,
      "secret": false
    },
    {
      "name": "EXPERTMESH_PLUGIN_CODE_REPOSITORY_MANAGER_SSH_PRIVATE_KEY",
      "description": "Optional SSH private key used by the code repository manager for SSH Git remotes.",
      "required": false,
      "secret": true
    },
    {
      "name": "EXPERTMESH_PLUGIN_CODE_REPOSITORY_MANAGER_SSH_KNOWN_HOSTS",
      "description": "Optional known_hosts content used with the SSH private key.",
      "required": false,
      "secret": true
    }
  ],
  "capabilities": [
    {
      "type": "document",
      "name": "code-repositories.md",
      "description": "Exposes configured repository metadata to the Agent document index."
    },
    {
      "type": "managed-tool",
      "name": "code_repository_manager_prepare_git",
      "description": "Checks that Git CLI and configured repository credentials are available."
    },
    {
      "type": "managed-tool",
      "name": "code_repository_manager_ensure_repository",
      "description": "Clones or fetches a configured repository into the Agent workspace."
    }
  ],
  "permissions": {
    "filesystem": ["workspace:read", "workspace:write"],
    "shell": ["git"],
    "network": ["configured git remotes"],
    "environment": ["configured credential variables"]
  }
}
