{
  "schemaVersion": "pragma.plugin/v2",
  "id": "learning-plugin",
  "name": "Learning Plugin",
  "description": "A teaching plugin that contributes context, a managed tool, and lifecycle hooks.",
  "version": "1.0.0",
  "tags": ["example", "context", "tool", "hooks"],
  "runtime": {
    "type": "expert-agent-plugin",
    "entry": "./src/plugin.ts",
    "trust": "trusted-host"
  },
  "capabilities": [
    {
      "type": "context",
      "name": "learning-plugin/team.md",
      "description": "Exposes configured team information as Agent context."
    },
    {
      "type": "managed-tool",
      "name": "create_team_greeting",
      "description": "Creates a greeting using plugin configuration."
    },
    {
      "type": "hook",
      "name": "lifecycle",
      "description": "Prints session, task, and tool lifecycle events."
    }
  ],
  "configuration": {
    "type": "object",
    "properties": {
      "teamName": {
        "type": "string",
        "description": "Team name exposed through context and tools.",
        "minLength": 1,
        "default": "Pragma Team"
      },
      "greetingPrefix": {
        "type": "string",
        "description": "Prefix used by create_team_greeting.",
        "minLength": 1,
        "default": "你好"
      }
    },
    "additionalProperties": false
  },
  "permissions": {
    "filesystem": [],
    "shell": [],
    "network": [],
    "environment": []
  }
}
