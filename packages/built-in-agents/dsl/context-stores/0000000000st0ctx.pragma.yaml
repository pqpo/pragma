apiVersion: pragma/v3
kind: ContextStore
metadata:
  id: 0000000000st0ctx
  name: Store revision target
  description: Host-provided read-only target for the built-in Store Revision Agent.
  tags: [builtin, system, context-store, revision]
spec:
  adapter: pragma.context.host@v1
  binding: binding:pragma.store-revision-target
  config:
    key: pragma.store-revision-target
